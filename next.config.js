/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/matthewasloan95.github.io', // Replace with your actual repo name
    images: {
      unoptimized: true,
    },
  }
  
  module.exports = nextConfig